// PostCSS config — Tailwind CSS v4 diproses oleh @tailwindcss/vite plugin,
// bukan via PostCSS. File ini hanya mempertahankan autoprefixer untuk vendor prefix.
export default {
    plugins: {
        autoprefixer: {},
    },
};
